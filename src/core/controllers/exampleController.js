class ExampleController {
    constructor(selector, sdk) {
        this._selector = selector;
        this._sdk = sdk;
    }

    getExamples = async (store, action) => {
        try {

        } catch {

        }
    }


}

export default ExampleController;